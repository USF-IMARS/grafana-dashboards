# grafana-dashboards
JSON exports of IMaRS grafana dashboards

Exported by `grafana_export_dashes.sh` in cronjob set up by imars_puppet.

Filename format is `${org_id}_${folder_title}_${dashboard_title}.json`.
